from setuptools import setup, find_packages

setup(
    name="mle",
    version="0.1.0",
    description="ds-workbench",
    author="manualrg",
    packages=find_packages(),            # Automatically find all packages in your project
    install_requires=[
        "requests>=2.20.0",
        "numpy>=1.18.0"
    ]
)
